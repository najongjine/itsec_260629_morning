package itsecmorning_2607;


class A{
	// public은 아무 scope에서나 접근 가능
	int v4;
	public int v=1;
	// 상속 관계 scope 에서는 접근 가능
	protected int v2=2;
	// 자기 자신만 접근 가능
	private int v3=3;
			
	
	/* 생성자.
	 * 자신의 클래스이름과 똑같은 method 이름.
	 * 만약 생성자를 안만들면, 컴파일러가
	 * 껍데기 생성자를 지가 만들어요 */
	A(int v){v=3;}
	
	void showvalue() {
		/* 문자열 + 데이터 := 문자열 붙이기 */
		System.out.println("v:"+v);
	}
}
// 상속. 나중에 배울거임
class B extends A{
	B(){
		super(1);
	}
	void change() {
		v2=3;
	}
}

public class itsecmorning_2607 {
	
	public static void main(String[] args) {
		A a1= new A(2);
		a1.v4=4;
		/* 다른 scope 에서는 protected, private 접근 불가능
		a2.v2=2;
		*/


	}

}

---------------------

package itsecmorning_2607;


public class itsecmorning_2607 {
	public static void main(String[] args) {
		/* 자바도 c언어처럼 문자열은 시작주소를 저장하는 형태로 가요
		 * 처음 등장하는 문자열은 heap 영역에 만들고
		 * 똑같은 문자열 나오면 재사용.
		 * new 나 배열을 사용하면 무조건 새로운 영역 만들어요 */
		String str1="abc";
		String str2="abc_";
		String str3="abc";
		String str4=new String("abc");
		
		// equals는 주소 비교하는게 아니라 내용물 비교하는 놈이에요
		System.out.println(str1.equals(str4));

	}

}
